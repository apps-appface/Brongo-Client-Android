<resources>

    <!-- Base theme applied no matter what API -->
    <style name="MyTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/appPrimaryBlue</item>
        <item name="colorPrimaryDark">@color/appPrimaryBlue</item>
        <item name="colorAccent">@color/appPrimaryBlue</item>
        <item name="android:statusBarColor">@color/appPrimaryBlue</item>
        <item name="android:textAllCaps">false</item>
        <item name="buttonStyle">@style/BrongoButton</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!--<item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowEnableSplitTouch">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>-->
        <!-- <item name="android:windowFullscreen">true</item>-->
    </style>

    <style name="AppTheme.NoACBar" parent="MyTheme">

    </style>

    <style name="BrongoButton" parent="@android:style/Widget.Button.Small">
        <item name="android:background">@drawable/brongo_button_drawable</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:paddingLeft">33dp</item>
        <item name="android:paddingRight">33dp</item>
        <item name="android:paddingTop">15dp</item>
        <item name="android:paddingBottom">15dp</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">21sp</item>
    </style>

    <style name="TextLabel">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/appPurple</item>
        <item name="colorControlNormal">@color/appPurple</item>
        <item name="colorControlActivated">@color/appPurple</item>
        <!--<item name="android:textSize">20sp</item>-->
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/appPrimaryFont</item>
        <item name="colorControlActivated">@color/starYellow</item>
    </style>

    <style name="CustomProgressDialog" parent="android:Theme.Holo.Dialog">
        <item name="android:alertDialogStyle">@style/CustomAlertDialogStyle</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="CustomAlertDialogStyle">
        <item name="android:bottomBright">@color/transparent</item>
        <item name="android:bottomDark">@color/transparent</item>
        <item name="android:bottomMedium">@color/transparent</item>
        <item name="android:centerBright">@color/transparent</item>
        <item name="android:centerDark">@color/transparent</item>
        <item name="android:centerMedium">@color/transparent</item>
        <item name="android:fullBright">@color/transparent</item>
        <item name="android:fullDark">@color/transparent</item>
        <item name="android:topBright">@color/transparent</item>
        <item name="android:topDark">@color/transparent</item>
    </style>

    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:windowBackground">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:background">@color/transparent</item>
    </style>

    <style name="ApplozicTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/appPrimaryBlue</item>
        <item name="colorPrimaryDark">@color/appPrimaryBlue</item>
        <item name="colorAccent">@color/appPrimaryBlue</item>
        <item name="windowActionModeOverlay">true</item>
    </style>

    <!--    For GifView Starts-->
    <style name="Widget.GifView" parent="@android:style/Widget" />

    <style name="AppTheme.NoACBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoACBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!--    For GifView Ends-->

    <style name="MyRadioButton" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/appPurple</item>
        <item name="colorControlActivated">@color/appPurple</item>
    </style>

</resources>
